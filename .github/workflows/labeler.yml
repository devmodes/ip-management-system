name: "Pull Request Labeler"
on:
  pull_request:
    branches:
      - main
      - dev

jobs:
  labeler:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
      - id: label-the-pr
      - uses: actions/labeler@v5
